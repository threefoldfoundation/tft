apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    backupType: {{ .Values.threefoldVdc.backup | quote }}
  name: tftbscbridge-volume-claim-{{ .Release.Name }}
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.volume.size }}